const { BaseAgent } = require('./agents/base-agent');
const { WebTesterAgent } = require('./agents/web-tester');
const { QualityInspectorAgent } = require('./agents/quality-inspector');

module.exports = {
  BaseAgent,
  WebTesterAgent,
  QualityInspectorAgent,
};
